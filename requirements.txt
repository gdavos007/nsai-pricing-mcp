# Pinned: the SSE transport's Host-header validation and transport_security
# API differ across versions — unpinned drift broke the Railway deploy.
mcp==1.28.1
httpx>=0.27.0
beautifulsoup4>=4.12.0
openpyxl>=3.1.0
lxml>=5.0.0
pandas>=2.0.0
uvicorn>=0.30.0
starlette>=0.37.0
